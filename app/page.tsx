import Link from 'next/link'
import { ArrowRight, BookOpen, Library, Search, Sparkles } from 'lucide-react'
import { publicDescription, books, publicNav } from '@/lib/biblio-data'

export default function HomePage() { return <main className="min-h-screen bg-background text-foreground"><header className="mx-auto flex max-w-6xl items-center justify-between px-6 py-6"><Link href="/" className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-primary text-primary-foreground"><Library className="size-5" /></span><span><span className="block font-semibold">BiblioNexus</span><span className="block text-[10px] uppercase tracking-[.16em] text-muted-foreground">Library intelligence</span></span></Link><nav className="flex items-center gap-3 text-sm text-muted-foreground sm:gap-6">{publicNav.map((item) => <Link key={item.href} href={item.href} className="hover:text-foreground">{item.label}</Link>)}<div className="flex items-center gap-3"><Link href="/sign-in" className="text-muted-foreground hover:text-foreground">Access</Link><Link href="/sign-up" className="rounded-lg bg-primary px-4 py-2 font-medium text-primary-foreground">Register</Link></div></nav></header><section className="mx-auto grid max-w-6xl gap-12 px-6 pb-20 pt-16 lg:grid-cols-[1.1fr_.9fr] lg:items-center"><div><p className="mb-5 flex items-center gap-2 text-xs font-semibold uppercase tracking-[.18em] text-primary"><Sparkles className="size-4" /> Your library, connected</p><h1 className="max-w-3xl text-balance text-5xl font-semibold tracking-[-.06em] sm:text-7xl">Find the next book that stays with you.</h1><p className="mt-6 max-w-xl text-lg leading-8 text-muted-foreground">{publicDescription} A quieter, more thoughtful way to explore everything your library has to offer.</p><form action="/opac/search" className="mt-8 flex max-w-xl items-center gap-3 rounded-2xl border border-border bg-card p-2 shadow-sm"><Search className="ml-3 size-5 text-muted-foreground" /><input name="q" placeholder="Search by title, author, or ISBN" className="min-w-0 flex-1 bg-transparent px-2 py-3 text-sm outline-none" /><button className="rounded-xl bg-primary px-5 py-3 text-sm font-medium text-primary-foreground">Search</button></form><div className="mt-6 flex flex-wrap gap-3 text-xs text-muted-foreground"><span className="rounded-full border border-border px-3 py-2">Open now · Central Library</span><span className="rounded-full border border-border px-3 py-2">48,291 items</span></div></div><div className="rounded-[2rem] border border-border bg-muted/30 p-6"><div className="mb-5 flex items-center justify-between"><span className="text-xs font-semibold uppercase tracking-[.16em] text-muted-foreground">Featured collection</span><BookOpen className="size-5 text-primary" /></div><div className="grid gap-3">{books.slice(0, 3).map((book, index) => <Link href={`/opac/book/${book.id}`} key={book.id} className="flex items-center gap-4 rounded-xl border border-border bg-card p-4 transition-transform hover:-translate-y-0.5"><span className="grid size-10 place-items-center rounded-lg bg-primary/10 text-sm font-semibold text-primary">0{index + 1}</span><span className="min-w-0 flex-1"><span className="block truncate font-medium">{book.title}</span><span className="block text-xs text-muted-foreground">{book.author}</span></span><ArrowRight className="size-4 text-muted-foreground" /></Link>)}</div><Link href="/opac/search" className="mt-5 flex items-center gap-2 text-sm font-medium text-primary">Explore catalog <ArrowRight className="size-4" /></Link></div></section></main> }
